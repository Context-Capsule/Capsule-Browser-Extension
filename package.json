{
  "name": "capsule-firefox-extension",
  "version": "0.1.12",
  "private": true,
  "description": "Firefox/Zen and Chrome browser adapter for Context Capsule",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "build": "node scripts/build.mjs firefox",
    "build:firefox": "node scripts/build.mjs firefox",
    "build:chrome": "node scripts/build.mjs chrome",
    "build:all": "pnpm build:firefox && pnpm build:chrome",
    "dev": "node scripts/dev.mjs",
    "dev:isolated": "node scripts/dev-browser.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node scripts/test.mjs && node scripts/test-tab-order.mjs && node scripts/test-splits.mjs && node scripts/test-order.mjs && node scripts/test-chrome-target.mjs",
    "test:build-targets": "node scripts/test-build-targets.mjs",
    "lint": "pnpm build && node scripts/lint.mjs",
    "check": "pnpm typecheck && pnpm test && pnpm lint && pnpm build:chrome && pnpm test:build-targets",
    "package": "pnpm build && web-ext build --source-dir dist --artifacts-dir web-ext-artifacts --overwrite-dest",
    "package:firefox": "pnpm package"
  },
  "packageManager": "pnpm@11.22.0",
  "dependencies": {
    "@samasante/liquid-glass": "0.1.1",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@types/firefox-webext-browser": "143.0.0",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.4",
    "esbuild": "0.28.2",
    "typescript": "7.0.2",
    "web-ext": "10.6.0"
  }
}
